// ScoringSpades configuration.
// When forking, set your own Google Analytics ID here (or leave blank to
// disable analytics entirely — no gtag script loaded, no events sent).
//
// AdSense ownership is asserted via a <meta name="google-adsense-account">
// tag in the <head> of each page (verification requires the tag in raw
// HTML, not JS-injected). To use your own publisher ID in a fork,
// find/replace ca-pub-3750434343270810 across index.html, how-to-play.html,
// and privacy.html — or delete those meta tags to disable ads.

window.SCORING_CONFIG = {
  // Google Analytics 4 measurement ID. Set to '' to disable.
  gaId: '',
};
